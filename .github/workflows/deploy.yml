name: Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: []  # TODO: add ci job dependency once ready
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      # TODO: Phase 6 — Build Docker images, push to ECR, Terraform apply, update ECS
      - name: Placeholder
        run: echo "Deploy pipeline not yet configured"
